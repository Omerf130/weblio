export type LeadActionState = {
  error?: string;
  success?: boolean;
};

export type LeadNotesActionState = {
  error?: string;
  saved?: boolean;
};

export type QualificationActionState = {
  error?: string;
  success?: boolean;
};
